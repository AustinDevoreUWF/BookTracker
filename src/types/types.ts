type User = {
    id: number,
    isAdmin: boolean,
    name: string,
    email: string,
    password: string,
}
type Book = {
    id: number,
    name: string,
    author: string,
    pubDate: Date,
    pageCount: number,
    
}